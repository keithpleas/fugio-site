# Deploy static site to Azure Web App (no MSBuild)
name: Deploy Fugio site to Azure Web App - fugio

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: windows-latest
    permissions:
      id-token: write      # for azure/login (federated credentials)
      contents: read       # for actions/checkout

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Stage only the website files (e
